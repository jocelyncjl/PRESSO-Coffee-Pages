// import React from 'react';

// const UserIcon = ({ name, className = '' }) => {
//   const iconSrc = require(`../assets/images/${user}.png`);

//   return <img src={iconSrc} alt={user} className={className} />;
// };

// export default UserIcon;